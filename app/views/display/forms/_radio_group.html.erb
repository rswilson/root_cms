<div class="field_radio_group">
	<% for option in radio_group.field_options do %>
	  <input type="radio" name="form_submission[form_submission_fields_attributes][<%= i %>][value]" value="<%= option.value %>" <% if option.value ==  submitted_field.value || option.value == radio_group.default_value %>checked<% end %> /><%= option.value %>
	<% end %>
</div>
